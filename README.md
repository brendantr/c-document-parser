# c-document-parser
C practice project that parses flat text into dynamically allocated document, paragraph, sentence, and word structs. Uses pointers, nested arrays, malloc, delimiter-based string parsing, null-terminated strings, and indexed document queries. Inspired by a HackerRank exercise. 
